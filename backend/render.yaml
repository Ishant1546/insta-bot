
services:
  - type: web
    name: insta-bot-backend
    env: python
    buildCommand: "pip install -r requirements.txt && playwright install webkit"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port 10000"
